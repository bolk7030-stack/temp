#!/bin/sh

WAN_DEV="eth1"
IFB_DEV="ifb-eth1"

GAME_CLASS="1:20"
BULK_CLASS="1:60"

SLEEP_SEC=1

GAME_UP=12000
GAME_DOWN=16000

BULK_UP_BASE=45000
BULK_DOWN_BASE=90000

BULK_UP_MIN=24000
BULK_DOWN_MIN=52000

TARGET_DELAY_MS=12

# Softer trim strength
UP_TRIM_PER_DEBT=400
DOWN_TRIM_PER_DEBT=620

PRESSURE_DEBT=0
LAST_PRESSURE_DEBT=0
PRESSURE_DEBT_MAX=60
PRESSURE_DECAY_PER_TICK=2

# Saturation + hardness restore
SAT_NUM=60
SAT_DIVISOR=36

# How much of the saturated-away edge to restore
# 2 = restore half
# 3 = restore one-third
HARDNESS_RESTORE_DIV=2

CUR_BULK_UP="$BULK_UP_BASE"
CUR_BULK_DOWN="$BULK_DOWN_BASE"

get_delay_ms() {
    DEV="$1"

    tc -s qdisc show dev "$DEV" 2>/dev/null | awk '
        /avg_delay/ {
            for (i=1; i<=NF; i++) {
                if ($i ~ /avg_delay/) {
                    val=$(i+1)
                    gsub(/[^0-9.]/, "", val)
                    print int(val)
                    exit
                }
            }
        }
        /delay/ {
            for (i=1; i<=NF; i++) {
                if ($i ~ /delay/) {
                    val=$(i+1)
                    gsub(/[^0-9.]/, "", val)
                    print int(val)
                    exit
                }
            }
        }
    '
}

clamp() {
    VAL="$1"
    MIN="$2"
    MAX="$3"

    [ "$VAL" -lt "$MIN" ] && VAL="$MIN"
    [ "$VAL" -gt "$MAX" ] && VAL="$MAX"

    echo "$VAL"
}

apply_class() {
    DEV="$1"
    CLASS="$2"
    RATE="$3"

    tc class change dev "$DEV" classid "$CLASS" \
        hfsc ls rate "${RATE}kbit" ul rate "${RATE}kbit" 2>/dev/null
}

apply_rates() {
    apply_class "$WAN_DEV" "$GAME_CLASS" "$GAME_UP"
    apply_class "$IFB_DEV" "$GAME_CLASS" "$GAME_DOWN"

    apply_class "$WAN_DEV" "$BULK_CLASS" "$CUR_BULK_UP"
    apply_class "$IFB_DEV" "$BULK_CLASS" "$CUR_BULK_DOWN"

    echo "APPLY delay=${DELAY}ms side=$SIDE debt=$PRESSURE_DEBT vel=$DEBT_VELOCITY kin=$KIN_DEBT raw=$RAW_EDGE sat=$SAT_EDGE hard=$HARDNESS final=$FINAL_EDGE game=${GAME_UP}/${GAME_DOWN} bulk=${CUR_BULK_UP}/${CUR_BULK_DOWN}"
}

apply_rates

while true
do
    WAN_DELAY="$(get_delay_ms "$WAN_DEV")"
    IFB_DELAY="$(get_delay_ms "$IFB_DEV")"

    [ -z "$WAN_DELAY" ] && WAN_DELAY=0
    [ -z "$IFB_DELAY" ] && IFB_DELAY=0

    if [ "$WAN_DELAY" -gt "$IFB_DELAY" ]; then
        DELAY="$WAN_DELAY"
        SIDE="wan"
    else
        DELAY="$IFB_DELAY"
        SIDE="ifb"
    fi

    EXCESS=$((DELAY - TARGET_DELAY_MS))

    if [ "$EXCESS" -gt 0 ]; then
        PRESSURE_DEBT=$((PRESSURE_DEBT + EXCESS))
    else
        PRESSURE_DEBT=$((PRESSURE_DEBT - PRESSURE_DECAY_PER_TICK))
    fi

    PRESSURE_DEBT="$(clamp "$PRESSURE_DEBT" 0 "$PRESSURE_DEBT_MAX")"

    DEBT_VELOCITY=$((PRESSURE_DEBT - LAST_PRESSURE_DEBT))
    [ "$DEBT_VELOCITY" -lt 0 ] && DEBT_VELOCITY=0

    KIN_DEBT=$((PRESSURE_DEBT * PRESSURE_DEBT / PRESSURE_DEBT_MAX))

    RAW_EDGE=$((KIN_DEBT + DEBT_VELOCITY))

    # Saturation controls flooding / over-tightening
    SAT_EDGE=$((RAW_EDGE * SAT_NUM / (RAW_EDGE + SAT_DIVISOR)))

    # Reverse-saturation hardness:
    # restore part of the edge that saturation removed,
    # so high-end bite is not deprived.
    HARDNESS=$((RAW_EDGE - SAT_EDGE))
    [ "$HARDNESS" -lt 0 ] && HARDNESS=0

    FINAL_EDGE=$((SAT_EDGE + HARDNESS / HARDNESS_RESTORE_DIV))
    FINAL_EDGE="$(clamp "$FINAL_EDGE" 0 "$PRESSURE_DEBT_MAX")"

    NEW_UP=$((BULK_UP_BASE - FINAL_EDGE * UP_TRIM_PER_DEBT))
    NEW_DOWN=$((BULK_DOWN_BASE - FINAL_EDGE * DOWN_TRIM_PER_DEBT))

    CUR_BULK_UP="$(clamp "$NEW_UP" "$BULK_UP_MIN" "$BULK_UP_BASE")"
    CUR_BULK_DOWN="$(clamp "$NEW_DOWN" "$BULK_DOWN_MIN" "$BULK_DOWN_BASE")"

    apply_rates

    LAST_PRESSURE_DEBT="$PRESSURE_DEBT"

    sleep "$SLEEP_SEC"
done